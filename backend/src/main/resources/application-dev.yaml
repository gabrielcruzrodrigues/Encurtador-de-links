spring:
  datasource:
    username: admin
    url: jdbc:postgresql://localhost:5432/encurtalink
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG

info: ============== develop ==============

